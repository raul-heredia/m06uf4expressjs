extends layout

block content
  div(class="flex items-center justify-center")
    div(class="px-40 py-20 bg-white rounded-md shadow-xl")
      div(class="flex flex-col items-center")
        h1(class="font-bold text-blue-600 text-9xl") 404
        h6(class="mb-2 text-2xl font-bold text-center text-gray-800 md:text-3xl")
          span(class="text-red-500") Oops! 
          | Pàgina no Trobada
        p(class="mb-8 text-center text-gray-500 md:text-lg")
          | La pàgina que estàs buscant no existeix.
        a(href="javascript:history.back()" class="px-6 py-2 text-sm font-semibold text-blue-800 bg-blue-100") Tornar Enrere
